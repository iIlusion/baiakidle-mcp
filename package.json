{
  "name": "baiakidle-mcp",
  "version": "1.0.0",
  "description": "Local MCP bridge for inspecting the BaiakIdle page and WebSocket traffic.",
  "type": "module",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/iIlusion/baiakidle-mcp.git"
  },
  "scripts": {
    "dev": "node scripts/dev.mjs",
    "build": "npm run build:bridge && npm run build:mcp",
    "build:bridge": "vite build && node scripts/add-userscript-banner.mjs",
    "build:mcp": "npm --prefix mcp-server run build",
    "typecheck": "tsc -p tsconfig.json --noEmit && npm --prefix mcp-server run typecheck",
    "test": "npm --prefix mcp-server test"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "vite": "^7.0.0"
  },
  "license": "MIT"
}